# 인수인계 — 창의디자인캠프 opencode 개조판

작성 2026-09-08 · 대상 후임자 · 작성자 이지호(sh.cho@pusan.ac.kr)

이 문서는 **코드를 읽어도 알 수 없는 것**만 담았습니다.
무엇이 어떻게 돌아가는지는 `README.md` 와 각 파일 머리말 주석에 이미
자세히 적혀 있습니다. 여기에는 그 주석에 없는 **맥락·사고 이력·미완성 항목**을
적었습니다.

> 표기 규칙: 확인하지 못한 것은 **(추정)** 이라고 붙였습니다.
> 붙어 있지 않은 것은 2026-09-05 현장에서 실제로 실행해 확인한 것입니다.

---

## 1. 한 장 요약

초등학교 5학년~중학교 1학년 **60~63명, 15개 조**가 이틀 동안
"과학 캠페인 웹슬라이드 발표자료"를 만드는 캠프의 도구 일체입니다.

학생은 코드를 보지 않습니다. 한국어로 대화만 합니다.
`opencode desktop 1.18.20` 을 **포크하지 않고**, 설정 층
(`~/.config/opencode`)만 갈아끼워 캠프 전용으로 만들었습니다.

**캠프는 2026-09-19~20 이고, 이 폴더의 마지막 작업은 2026-09-05 현장에서
이루어졌습니다.** 즉 캠프 리허설/사전 현장 운영을 한 번 치른 상태이고,
그때 터진 것들을 급하게 고친 흔적이 코드 전체에 남아 있습니다.

---

## 2. 어디까지 됐나

### 동작하는 것 (실제로 실행해 확인)

| 항목 | 상태 |
|---|---|
| 윈도우 원클릭 설치 (`camp2026-setup-offline.exe`, 375MB) | 처음부터 끝까지 통과. 인터넷 없이 설치됨 |
| 설치 자체 점검 6항목 | 전부 초록 확인 |
| 그림 만들기 (Gemini) | 8초 / 약 800KB JPEG |
| 대표 이미지 (Gemini) | 25초 / 2752×1536 |
| 영상 만들기 | 40~50초 / 1.5~2.3MB MP4 (구글), 2분 30초 / 9.5MB (fal) |
| 15개 조 동시 그림 생성 | 15/15 성공, 11초, 429 없음 |
| 발표자료 합치기 (`merge-slides`) | 정상 |
| 내부망 배포 서버 (`lan-serve.py`) | 45개 동시 접수 무손실, 학생 실제 다운로드 성공 |
| 「도구 고치기」 (윈도우) | 고장난 상태를 만들어 놓고 실행 → 전부 복구 확인 |
| 도우미가 사진 보기 | 처음 보는 그림의 개수·색·주변 사물까지 정확히 서술 |
| 발표자료 배경에 사진 깔기 | 브라우저에 띄워 눈으로 확인 |
| 테스트 | PowerShell 14개 파일 / bash 15개 파일 **전부 통과** |

### 안 되는 것 · 확인 못 한 것 (여기가 중요합니다)

| 항목 | 상태 |
|---|---|
| **맥 실물 테스트** | **한 번도 못 했습니다.** 맥이 없었습니다. 로직은 부분적으로 떼어내 검증했지만, `ditto` 압축 풀기·`osascript` 앱 종료·앱 실행은 미확인 |
| **Windows 10 1703 에서 앱이 열리는지** | 미확인. CLI 는 확실히 안 됩니다(아래 함정 참조). 앱(Electron)은 별개 바이너리라 열릴 **가능성**만 있습니다 |
| **camp.kodekorea.kr 에 올라간 파일이 최신이 아닙니다** | 2026-09-05 11:38 빌드본입니다. 그 뒤 현장에서 고친 것(관리자 권한, 영상, 비전 모델 등)은 **내부망으로만** 배포했습니다 |
| Veo fast/standard 단가 | 확인 못 했습니다. lite 는 4초 720p 약 $0.20 |
| fal 단가·크레딧 잔액 | 확인 못 했습니다 |
| AGENTS.md 변경이 **진행 중인 대화**에 반영되는지 | 카나리아를 심어 시험했는데 결론이 안 났습니다. 모델이 무시한 것인지 못 읽은 것인지 구분 불가. 그래서 "새 대화를 열게 하는" 우회로를 넣었습니다 |
| 맥 Electron userData 폴더 이름 | `ai.opencode.desktop` 일 것으로 **(추정)**. 윈도우가 그 이름이라 맞출 가능성이 크지만 맥에서 확인 못 했습니다 |
| 커밋 안 된 변경 | 2026-09-05 현장 수정 다수가 **커밋되지 않았습니다**. 원본 리포에서 확인·커밋해야 합니다 |

---

## 3. 파일·폴더 지도

### 루트 (여기가 소스입니다)

| 경로 | 무엇인가 | 왜 여기 있나 |
|---|---|---|
| `camp-preset/` | opencode 설정 프리셋. 에이전트 4 · 명령 11 · 스킬 5 | **학생이 쓰는 AI의 성격이 전부 여기 있습니다.** 프롬프트를 고치려면 여기 |
| `template/` | 팀 발표자료 템플릿 (`index.html` + `slides/`) | 조 폴더를 만들 때 그대로 복사됩니다 |
| `scripts/` | 빌드 스크립트 + 학생이 실제로 실행하는 도구 | 두 종류가 섞여 있습니다. `build-*.ps1` 은 주최측용, `camp-*.sh`/`*.py` 는 학생 노트북으로 배포됩니다 |
| `dist/` | 학생에게 전달되는 배포판 **staging 폴더** | 소스와 생성물이 섞여 있습니다. 아래 별도 표 참조 |
| `tests/` | 검증 (bash 15 + PowerShell 14 파일) | 빌드 전에 돌리세요. 여러 사고를 여기서 잡았습니다 |
| `docs/운영/` | 사전교육 진행안 · 학생 치트시트 · 멘토 트러블슈팅 · 학부모 안내문 · 영상 대본 | 운영 문서. 캠프 진행자에게 그대로 주는 것 |
| `docs/superpowers/` | 설계 문서 (spec / plan) | 처음 설계 근거. 지금 코드와 일부 어긋납니다 |
| `deploy/` | `camp.kodekorea.kr` Caddy 블록 + 안내 웹페이지 | 서버 설정 사본. 서버에 직접 붙여 넣었습니다 |
| `_보관/` | 다시 만들 수 있는 것 전부 | `_보관/읽어보세요.txt` 를 먼저 보세요 |

### `dist/` 안 — 소스와 생성물이 섞여 있습니다

| 경로 | 종류 | 비고 |
|---|---|---|
| `dist/scripts/*.ps1` | **소스** (손으로 쓴 것) | 설치·점검·실행의 실제 구현. 가장 중요한 코드 |
| `dist/*.cmd` | **소스** | 비상용 진입점. 순수 ASCII 여야 합니다 |
| `dist/secrets/` | **열쇠** (git 제외) | 아래 5장 참조 |
| `dist/*.exe` | 생성물 | `build-exe.ps1` 이 만듭니다 |
| `dist/preset/`, `dist/template/`, `dist/tools/` | 생성물 (복사본) | `build-dist.ps1` 이 루트에서 복사합니다. **여기를 고치면 다음 빌드에 덮어써집니다** |
| `dist/offline/*.zip` | 생성물 | `make-offline-payload.ps1` 이 만듭니다 |
| `dist/bundle/` | **지금 없습니다** | `_보관/05_번들_내려받은설치파일/bundle` 로 옮겼습니다. 빌드 전에 되돌리세요 |

---

## 4. 실행·빌드·배포 절차

### 준비물

- Windows 10/11 64비트, PowerShell 5.1 (기본)
- Git for Windows (git bash 필요 — `.sh` 도구가 이걸로 돕니다)
- Python 3.12 (실측 경로: `%LOCALAPPDATA%\Programs\Python\Python312\python.exe`)
- `.NET Framework` 의 `csc.exe` (윈도우에 기본 포함). exe 를 이걸로 컴파일합니다
- 별도 툴체인·패키지 매니저는 **일부러** 안 씁니다

### 빌드 (순서가 중요합니다)

```powershell
# 0) 번들 되돌리기 (한 번만)
move "_보관\05_번들_내려받은설치파일\bundle" dist\bundle

# 1) 런처 exe 5개
powershell -ExecutionPolicy Bypass -File scripts\build-exe.ps1

# 2) 배포판 staging (preset/template/tools 복사 + 완성도 검사)
powershell -ExecutionPolicy Bypass -File scripts\build-dist.ps1

# 3) 오프라인 짐 (앱이 첫 실행 때 받아오는 70MB 를 미리 넣는다)
powershell -ExecutionPolicy Bypass -File scripts\make-offline-payload.ps1

# 4) 원클릭 설치 파일
powershell -ExecutionPolicy Bypass -File scripts\build-installer-exe.ps1
#    dist\offline\ 이 있으면      -> camp2026-setup-offline.exe
#    dist\legacy.flag 가 있으면   -> camp2026-setup-1703.exe
#    둘 다 없으면                 -> camp2026-setup.exe

# 5) 「도구 고치기」 + tools.zip  (이미 설치한 학생 갱신용)
powershell -ExecutionPolicy Bypass -File scripts\build-fixtools-exe.ps1

# 6) 맥 몫 (공식 파일 내려받기 + 프리셋 tar)
powershell -ExecutionPolicy Bypass -File scripts\build-mac.ps1
```

**1번을 건너뛰면 안 됩니다.** exe 는 불러올 스크립트 목록을 자기 안에
박아 둡니다. 스크립트만 고치고 exe 를 안 만들면 exe 가 없는 함수를 부르다
죽습니다. 그 사고를 실제로 냈고, 그래서 `build-dist.ps1` 에
"스크립트가 exe 보다 새것이면 빌드 중단" 검사를 넣었습니다.

### 테스트

```bash
# bash (git bash 에서)
cd <리포>
export CAMP_NO_SHOW=1          # 안 하면 브라우저가 계속 열립니다
for f in tests/test-*.sh; do bash "$f"; done
```
```powershell
# PowerShell
Get-ChildItem tests\*.ps1 | ForEach-Object {
  powershell -NoProfile -ExecutionPolicy Bypass -File $_.FullName
}
```

### 현장 배포 — 내부망 (이게 실제로 쓴 방법입니다)

행사장 **들어오는 회선이 100Mbps** 라 63명이 375MB 를 밖에서 받으면
회선이 죽습니다. 내부망은 1Gbps + 5GHz 였습니다.

```powershell
# 뿌릴 폴더를 통째로 서비스한다
python scripts\lan-serve.py lan-share 80
python scripts\lan-serve.py lan-share 8080    # 맥 안내용으로 하나 더 띄웠음
```

- 학생에게 알려준 주소: **`192.168.0.5`** (진행자 노트북)
- 방화벽 인바운드 80/8080 을 열어야 합니다 (관리자 권한 필요)
- 페이지에 나오는 것: 윈도우 큰 단추 / 「도구 고치기」 / 맥 터미널 3줄 / 1703 링크
- 실측 성능: 45개 동시 접수 무손실, 이어받기(Range) 지원

**서버를 재시작해야 하는 때** — `lan-share` 안의 파일을 바꿨을 때는
**반드시** 재시작하세요. 서버는 시작할 때 파일 크기·해시를 캐시합니다.
바꾸고 재시작하지 않으면 잘린 파일을 내보냅니다.
단, 재시작하면 받는 중인 학생은 끊기고 표딱지(ETag)가 바뀌어
**이어받기가 안 되고 처음부터 다시** 받습니다. 쉬는 시간에 하세요.

### 현장 배포 — USB

`E:` (라벨 `CAMP2026`, exFAT). 인터넷·내부망 없이도 설치됩니다.
윈도우는 `창의디자인캠프 설치.exe` 두 번 누르기,
맥은 `bash /Volumes/CAMP2026/mac-install.sh`.

> 이 USB 는 원래 Windows 11 설치 미디어였고, 사용자 확인을 받고 exFAT 으로
> 포맷했습니다. install.wim 은 지워졌습니다.

### 외부 배포 — camp.kodekorea.kr

```bash
ssh blend                          # = ubuntu@156.228.4.156, 키는 아래 5장
# 파일 위치: /var/www/camp/camp2026-setup.exe
# 방법: .new 로 올리고 → sha256 확인 → mv 로 원자적 교체
scp camp2026-setup-offline.exe blend:/var/www/camp/camp2026-setup.exe.new
ssh blend "sha256sum /var/www/camp/camp2026-setup.exe.new"
ssh blend "cd /var/www/camp && mv camp2026-setup.exe camp2026-setup.exe.old-<날짜> \
           && mv camp2026-setup.exe.new camp2026-setup.exe && chmod 755 camp2026-setup.exe"
```

- Caddy 설정은 `deploy/Caddyfile-camp-블록.txt` 그대로 붙여 넣었습니다
- 주소로 들어가면 **아무것도 누르지 않고 바로 받아집니다** (페이지 없음)
- **이 서버에 다른 도메인 16개가 함께 돌고 있습니다. 다른 블록은 절대 건드리지 마세요.**

---

## 5. 자격증명 — 값은 안 씁니다. 어디에 있는지만

| 무엇 | 어디에 |
|---|---|
| AI 대화 열쇠 (deepseek) | `dist/secrets/auth.json` → 학생 노트북 `~/.local/share/opencode/auth.json` |
| 그림·영상 열쇠 | `dist/secrets/media-keys.env` → 학생 노트북 `~/.config/camp/media-keys.env` |
| ↳ 들어 있는 항목 | `GEMINI_API_KEY`(사용중) · `FAL_KEY`·`FAL_KEYS`(5개, 사용중) · `CF_ACCOUNT_ID`·`CF_API_TOKEN`(비어 있음) |
| 같은 열쇠의 배포 사본 | `_보관/02_현장배포_lan-share/lan-share/tools.zip` 안 `keys/` , `camp-mac-payload.tar.gz` 안 `secrets/` |
| 서버 접속 | ssh alias `blend` (`~/.ssh/config`), 키 `~/.ssh/id_ed25519_new`, 계정 `ubuntu@156.228.4.156` |
| 예시 파일 | `scripts/media-keys.env.example` (값 없음, 형식만) |

### 반드시 알아야 할 것

- **API 열쇠가 배포 exe 안에 박혀 있습니다.** 이건 의도된 설계입니다
  (초등학생에게 열쇠를 입력시킬 수 없어서). 대신:
  - 리포는 **비공개**로 유지해야 합니다
  - exe·USB·`tools.zip` 을 **공개 링크에 올리면 안 됩니다**
  - `camp.kodekorea.kr` 은 `X-Robots-Tag noindex` 로 색인을 막아 뒀습니다
  - **캠프가 끝나면 열쇠를 전부 폐기해야 합니다** (아래 8장)
- fal 열쇠 5개는 2026-09-05 저녁에 **채팅으로 평문 전달받았습니다.**
  그 경로에 남아 있으므로 캠프 후 폐기가 특히 중요합니다.
- **미해결 플래그**: `lms.kodekorea.kr` 을 돌리는 서버의 `kode-lms` 컨테이너
  환경변수와 그 안에 같이 있던 `가이드_R2업로드_로그인.md` 에
  **R2 키·Supabase service-role 키·관리자 비밀번호·JWT 시크릿이 평문으로**
  들어 있습니다. 이 프로젝트와 별개 시스템이지만 발견해서 보고했고,
  **조치되지 않았습니다.** 후임이 확인해 주세요.

---

## 6. 상대 기관 · 일정 · 합의된 것

| 항목 | 내용 |
|---|---|
| 주최 | 코드코리아 (작성자가 최고운영진) |
| 행사명 | 2026 영남·제주권역 창의디자인캠프 |
| 일정 | **2026-09-19 ~ 09-20 (1박2일)** |
| 참가자 | 초5~중1, 60~63명, 15개 조 (조당 4명 내외) |
| 산출물 | 과학 캠페인 웹슬라이드 발표자료 (조별 1개) |
| 학생 장비 | **개인 노트북 지참.** 윈도우 우선, 맥 소수, 사양 편차 큼 |
| LMS | `lms.kodekorea.kr` — 관리자 계정 `gifted` 로 강좌 관리 |
| 배포 사이트 | `camp.kodekorea.kr` |
| 사전 온라인교육 | 2시간. 그때 학생이 집에서 혼자 설치합니다 (`docs/운영/사전온라인교육-2시간.md`) |

### 합의·결정 사항 (사용자가 직접 정한 것)

- **문의 창구를 두지 않습니다.** 집에서 설치가 안 되면 "여기서 그만하고
  캠프 첫날 아침에 오세요"로 통일했습니다. 실패 안내문에 연락처를 넣지
  마세요 (`tests/test-python-stub.ps1` 이 "선생님을 불러" 문구가 설치 경로에
  들어가는 것을 막습니다)
- **학생에게 시간을 숫자로 말하지 않습니다.** "5초" 같은 표현 대신
  "좀 걸려요". 노트북 성능이 달라 숫자가 거짓말이 됩니다
- **실명 관련 안내 문구를 전부 제거**하기로 했습니다 (프로그램·문서 어디에도)
- 앱 자동 업데이트는 캠프 끝날 때까지 **끕니다**
  (`app-update.yml` 이름을 바꿔 electron-updater 를 스스로 끄게 만듦)
- LMS 10번 강좌 "AI 에이전트 세팅법" 영상은 **교체 완료**.
  R2 업로드 + DB row 갱신 방식으로 했고 **학생 진행률은 유지**됩니다.
  옛 R2 파일은 지웠습니다 (로컬에 사본 있음)
- 설치 안내 영상: `창의디자인캠프_설치안내_전체.mp4` (47분 32초, 68.7MB).
  LMS 에 올라가 있습니다

### 사전교육 영상과 코드가 어긋나는 지점 (주의)

영상 7장에서 **"점검에서 초록불 여섯 개"** 라고 말합니다.
그래서 점검 항목 수를 **6개로 고정**해 두었습니다. 항목을 늘리려면
영상을 다시 찍어야 합니다. 새 검사는 기존 6항목 안에 접어 넣으세요.

---

## 7. 함정 — 재발하면 곤란한 것들

캠프 도중 실제로 터진 것들입니다. **여기가 이 문서에서 가장 값진 부분입니다.**

### 인코딩·줄바꿈 (제일 많이 물렸습니다)

1. **`opencode.json` 에 BOM 이 붙으면 앱이 창도 못 띄우고 죽습니다.**
   electron-store 가 `JSON.parse` 예외를 그대로 던집니다. A/B 로 확인:
   BOM 있으면 프로세스 3개·창 0개, 없으면 프로세스 6개·창 1개.
   → `lib-appstate.ps1` 의 `Write-JsonNoBom` 을 쓰세요.
2. **PowerShell 5.1 의 `Set-Content -Encoding utf8` 은 BOM 을 붙입니다.**
   위 사고의 원인이었습니다. `[System.IO.File]::WriteAllText` +
   `UTF8Encoding($false)` 를 쓰세요.
3. **`.ps1` 은 BOM 이 있어야 합니다.** 없으면 5.1 이 한글을 ANSI 로 읽어
   전부 깨집니다. `scripts/fix-ps1-bom.ps1` 이 있습니다.
4. **`.cmd` 는 순수 ASCII 만.** cmd.exe 가 UTF-8 배치의 비ASCII 를 잘못
   파싱해 명령이 쪼개집니다. 학생이 읽는 한글은 PowerShell 쪽에 두세요.
5. **`.sh`/`.py` 는 반드시 LF.** CRLF 면 맥에서 bash 가 모든 줄 끝에 `\r` 을
   붙여, `CFG="$HOME/.config/opencode"` 가 **`opencode<CR>` 폴더**를 만듭니다.
   눈으로는 똑같아 보입니다. 실제로 맥 한 대에서 설정·열쇠가 전부 엉뚱한
   폴더로 들어가 "대화가 안 돼요"가 됐습니다.
   → `build-dist.ps1` 이 `dist/tools` 의 CRLF 를 검사해 빌드를 멈춥니다.
   파이썬으로 파일을 쓸 때 `newline=''` 를 안 주면 윈도우에서 다시 CRLF 가
   됩니다. 실제로 그렇게 재발했습니다.

### bash

6. **bash 는 한글 변수 이름을 못 씁니다.** 이 프로젝트에서 **네 번** 물렸습니다
   (`시작=`, `절대=`, `후보=`, 그리고 새 스크립트들). `bash -n` 은 이걸 못
   잡습니다 — 실행해야 터집니다. 변수·반복문 이름은 전부 ASCII 로 쓰세요.
7. **`--옵션` 뒤 값이 빠지면 무한 루프.** `shift 2` 가 실패하고 `set -e` 가
   없어서 같은 인자를 영원히 다시 봅니다. CPU 100%, 출력 없음.
   `[ $# -ge 2 ] || { ...; exit 4; }` 가드를 모든 옵션에 넣었습니다.

### 윈도우 설치

8. **Microsoft Store 의 껍데기 `python.exe`.** `WindowsApps` 안의
   리다이렉터가 `Get-Command` 에 잡혀서, 파이썬이 안 깔렸는데 점검이
   초록불이었습니다. **63명 전원이 그림을 못 만들 상태였습니다.**
   → `Test-RealExe` 가 `WindowsApps` 경로를 거르고 실제로 실행해 봅니다.
9. **`msiexec` 경로 인용.** `Start-Process -ArgumentList` 는 배열을 공백으로
   이어 붙이고 인용을 안 해줍니다. 사용자 이름에 공백이 있으면 1619 로
   죽습니다. 실제로 재현했습니다.
10. **node MSI 는 관리자 권한이 필요합니다.** `ALLUSERS=0` 을 줘도 패키지
    안의 설치 폴더가 `ProgramFiles64Folder` 로 박혀 있습니다.
    그런데 **캠프 도구는 node 를 한 번도 안 씁니다** (앱은 Electron 내장,
    CLI 는 단일 바이너리, `.sh` 는 파이썬만). 그래서 실패하면 건너뛰고
    계속하게 했습니다. 이걸 `return 1` 로 두면 파이썬·앱·프리셋·
    **바탕화면 아이콘이 하나도 안 깔립니다.**
11. **SFX 에 `requireAdministrator` 를 붙이면 안 됩니다.** 학생 계정이
    비관리자일 때 부모님 계정으로 UAC 를 넘기면 **393MB 가 부모님
    프로필에 풀립니다.** 학생 쪽에는 다시 실행할 파일조차 없습니다.
    → SFX 는 `asInvoker`, **안쪽 `창의디자인캠프 설치.exe` 만** 관리자.
    UAC 는 여전히 시작하자마자 뜨고, 취소하면 껍데기가 잡아서 알려줍니다.
12. **exe 는 스크립트 목록을 자기 안에 박아 둡니다.** 스크립트를 새로 만들고
    `build-exe.ps1` 을 안 돌리면 exe 가 없는 함수를 부르다 죽습니다.
    `CreateNoWindow=true` 라 **화면에 아무것도 안 뜹니다.**
    이것 때문에 "설치가 안 되고 바탕화면에 아무것도 안 생긴다"가 났습니다.
    → 신선도 검사 + stderr MessageBox 를 넣었습니다.
13. **설치를 여러 번 하면 백업이 쌓입니다.** `.config/opencode.backup-*` 이
    한 번에 61MB(node_modules 포함). 개발 기계에서 5번에 226MB 가 쌓였습니다.
    → 우리가 깐 것이면 백업하지 않고 프리셋 폴더만 갈아끼웁니다.
14. **Windows 10 1703 은 근본적으로 안 됩니다.** opencode CLI 가
    `ClosePseudoConsole`(**1809 에서 처음 생긴 API**)을 씁니다. 실행하면
    "프로시저 시작 지점을 찾을 수 없습니다" 창이 뜹니다. 고칠 수 없습니다.
    → 1703 전용 빌드는 점검이 CLI 를 **아예 안 부르게** 만들었습니다.
    앱이 열리는지는 그 노트북에서 눌러 봐야 압니다 **(추정: 열릴 수도 있음)**.

### 맥

15. **`/usr/bin/python3` 는 껍데기입니다.** 개발자도구가 없어도 "있는" 것처럼
    보이고, 실행하면 **"개발자 도구를 설치할까요?" 창이 뜨고 1GB 를
    받습니다.** 100Mbps 회선에서 여럿이 하면 회선이 죽습니다.
    → `py_ok()` 가 `/usr/bin/*` 이면 `xcode-select -p` 로 먼저 걸러냅니다.
16. **python.org 판 파이썬은 인증서를 따로 넣어야 HTTPS 가 됩니다.**
    `Install Certificates.command` 를 실행하지 않으면 그림 만들기가 전부
    SSL 오류로 죽습니다. **설치 때는 안 보이고 캠프 당일에만 보입니다.**
17. **파인더로 연 맥 앱은 `~/.zshrc` 의 PATH 를 물려받지 않습니다.**
    그래서 `camp-media.sh` 를 못 찾습니다.
    → `~/.config/camp/campshell` 래퍼를 만들어 `opencode.json` 의 `shell` 로
    지정합니다. 그 래퍼가 PATH 와 **`LOCALAPPDATA`** 를 export 합니다.
18. **프리셋이 `$LOCALAPPDATA/Programs/camp-tools/...` 를 하드코딩합니다** (9곳).
    맥에는 그 변수가 없어 빈 경로가 됩니다. 프리셋을 맥용으로 따로 고치면
    두 벌을 관리해야 하므로, **맥에서 그 경로가 통하게** 만들었습니다
    (`LOCALAPPDATA=$HOME/.local` + `~/.local/Programs/camp-tools` 심볼릭 링크).
    → 프리셋에서 이 경로를 맨이름(`camp-media.sh`)으로 바꾸려는 시도를
    했다가 되돌렸습니다. `tests/test-commands.sh` 가 "PATH 의존 금지"를
    강제하는데, 그 규칙에는 이유가 있습니다(앱이 PATH 갱신 전에 떠 있으면
    맨이름이 안 잡힘).
19. **깨끗한 맥에는 `~/.zshrc` 가 없습니다.** "있으면 넘어간다"로 짜면
    PATH 가 영영 안 들어갑니다. 없으면 만들어야 합니다.
20. `say()` 같은 로그 함수가 실패를 반환하면 `... && ok || fail` 에서
    **성공과 실패가 둘 다 찍힙니다.** `return 0` 을 명시하세요.

### 앱 · 프리셋

21. **앱이 켜져 있으면 옛 설정을 붙들고 있습니다.** 창만 닫아도 프로세스가
    남습니다. "껐다 켜세요"를 글로 알려주면 아무도 안 읽습니다.
    → 「도구 고치기」가 **앱을 직접 종료**하고, `새대화필요.txt` 표를 남겨
    「캠프 시작」이 **새 대화를 강제로 열게** 했습니다.
22. **모델 설정은 이어지는 대화에도 즉시 반영됩니다** (실측). 하지만
    AGENTS.md·스킬 본문까지 그런지는 결론이 안 났습니다 **(추정: 불확실)**.
    그래서 설정을 바꾼 뒤에는 새 대화를 열게 하는 편이 안전합니다.
23. **도우미는 새 규칙을 알아도 이미 만든 발표자료를 되돌아가 고치지
    않습니다.** 그래서 「도구 고치기」가 예시 슬라이드를 **기계적으로**
    지웁니다(손대지 않은 것만, 표지는 남김).
24. **템플릿에 예시 슬라이드를 여러 장 넣으면 15개 조가 전부 그걸 조금씩만
    고쳐서 똑같이 만들어 옵니다.** 이게 "애들 웹사이트가 바보같이 나온다"의
    실제 원인이었습니다. → 표지 한 장만 남겼습니다.
    `tests/test-template.sh` 가 "표지 한 장"을 검사합니다.
25. **`merge-slides` 는 `<section>` 하나만 있으면 됩니다.** 그래서
    `<section>` 안에 `<style>` 을 넣어도 살아남습니다 — 조마다 완전히 다른
    레이아웃을 만드는 열쇠입니다. `<section>` 밖에 쓴 것은 사라집니다.
26. **앱은 대화 화면이 아니라 목록 화면으로 열립니다.** 학생에게 "세션"을
    설명할 수 없어서 `lib-uia.ps1` 이 UI Automation 으로 「새 세션」을 대신
    누릅니다. 좌표가 아니라 **이름**으로 찾습니다.
27. **이 앱에 글자를 넣는 방법은 UIA `ValuePattern.SetValue` 뿐입니다.**
    SendInput 유니코드·SendKeys·클립보드 붙여넣기 셋 다 아무 일도 안 났습니다
    (녹화 도구용 지식. 학생은 진짜 키보드를 쓰니 무관합니다).
28. **그림을 대화창에 마크다운으로 띄울 수 없습니다.** 그래서 도구가
    `_보기.html` 을 만들고 **브라우저**로 엽니다. 사진 앱으로 열면
    첫 실행 때 OneDrive 동의 창이 떠서 학생이 거기서 막힙니다.

### 미디어 생성 · 요금

29. **`durationSeconds` 를 문자열로 보내면 Veo 가 400 을 던집니다.**
    `int()` 여야 합니다. 이것 때문에 **영상 기능이 100% 죽어 있었습니다.**
    400 은 재시도 목록에 없어서 조용히 실패했습니다.
30. **Veo 할당량은 모델마다 따로 셉니다.** `veo-3.1-lite` 만 429 인데
    `fast`·`veo-3.1` 은 멀쩡한 상황이 실제로 있었습니다.
31. **fal 은 계정 단위 제한이고, 거절하지 않고 줄을 세웁니다.**
    측정: 45개 동시 접수 전부 성공, 동시 실행 최대 21~22, 상태 조회는
    초당 229회에도 안 막힘. **키를 5개 써도 처리량은 안 늘어납니다**
    (키 1개 12개 vs 키 5개 12개 → 최대 동시 처리 동일).
32. **제공자 순서가 곧 요금입니다.** `lite → fast → veo3.1 → fal` 로 두면
    lite 가 막힌 뒤 **비싼 fast 를 계속 쓰고 fal 은 안 씁니다.**
    실제로 그렇게 돌아서 fal 크레딧이 하나도 안 줄었습니다.
    → 지금 순서: **`lite → fal → fast → veo3.1`**
33. **기다리기를 포기해도 fal 작업은 계속 돌고 요금이 나갑니다.**
    → 시간 초과 시 취소 요청을 보내고, 대기 상한을 20분 → 8분으로 줄였습니다.
    단 **이미 실행 중인 작업은 취소해도 안 멈춥니다** (실측).
34. **동시에 그림 두 장을 만들면 한 장이 조용히 사라졌습니다.** 이름은 즉시
    고르는데 파일은 8~35초 뒤에 생겨서, 그 사이 다른 실행이 같은 빈 자리를
    봅니다. 세 번 다 "만들었어요"라고 말하고 한 장만 남습니다.
    → `set -o noclobber` 로 자리를 원자적으로 잡습니다.
35. **영어 오류·스택 트레이스·파일 경로를 학생에게 보여주면 안 됩니다.**
    11살은 거기서 멈춥니다. 기록에만 남기고 화면에는 한국어 한 줄.
36. **조당 영상 개수 제한이 유일한 지출 안전장치입니다.**
    `CAMP_VIDEO_MAX` (기본 2, `0` 이면 영상 완전 차단).

### 내부망 서버

37. **`lan-serve.py` 는 시작할 때 파일 목록·크기·해시를 캐시합니다.**
    파일을 바꾸고 재시작하지 않으면 잘린 파일이 나갑니다.
    한때 포트 80 이 **옛 빌드를 램에서 계속 뿌리고** 있었습니다.
    부분 비교로는 안 잡힙니다 — **전체 해시**를 떠야 보입니다.
38. **HTTP/1.1 keep-alive 에 타임아웃이 없으면 스레드가 안 죽습니다.**
    유휴 40개를 만들었더니 20초 뒤에도 스레드 42개가 그대로였습니다.
    → `timeout = 120` 을 클래스에 넣었습니다. 짧게 잡으면 느린 학생을 끊습니다.
39. **파일을 바꿔치기하면 이어받기가 깨진 파일을 만듭니다.** 브라우저는
    "73%부터 이어서"를 요청하는데 그 뒤가 다른 파일입니다. 학생은 깨진 줄
    모르고 두 번 누릅니다. → ETag + `If-Range` 로 표딱지가 다르면 처음부터
    다시 보냅니다.
40. **경로 탈출은 불가능합니다** (시작할 때 만든 화이트리스트 조회).
    8가지 벡터를 시험했고 전부 404 였습니다.
41. **curl 이 보내는 날것 한글 URL** 은 파이썬 http.server 가 latin-1 로
    읽어서 404 가 됩니다. 되돌려 놓는 코드를 넣었지만, **서비스 파일 이름은
    ASCII 로 두는 편이 안전합니다** (1703 빌드를 그래서 `win1703-setup.exe`
    로 바꿨습니다).

### 네트워크 (행사장)

42. 유선 `CDcamp-A` = 게이트웨이 `192.168.0.1`, 진행자 노트북 `192.168.0.5`.
    `192.168.0.2/.3/.4` 는 하위 공유기 B/C/D (전부 ipTIME).
    학생은 `.16`, `.28` 처럼 **같은 대역으로 직접 붙는 것**을 확인했습니다.
43. 하위 공유기가 라우터 모드여도 학생 → `192.168.0.5` 는 **NAT 아웃바운드**라
    통합니다. 유일하게 깨지는 경우는 하위 대역이 `192.168.0.x` 로 겹칠 때입니다
    (실측된 하위 대역은 `192.168.103.x` 라 안 겹쳤습니다).
44. **한 AP 에 40명 몰리면 5GHz 가 경합으로 무너집니다.** 조를 B/C/D 에
    분산시키세요. A→각 공유기는 기가 유선이라 병목이 아닙니다.

---

## 8. 다음에 해야 할 일

### 캠프 전에 반드시 (우선순위 순)

1. **맥 실물 1대로 전 과정 테스트.** 이게 가장 큰 미확인 구멍입니다.
   `mac-install.sh` → `mac-check.sh` → `mac-fix.sh` → `mac-uninstall.sh`.
   로그는 `~/창의디자인캠프/설치기록.txt`.
   특히 확인할 것: `ditto` 압축 풀기 / `osascript` 앱 종료 /
   `~/Applications` 에 넣은 앱이 열리는지 / `Install Certificates.command` /
   Electron userData 폴더 이름
2. **`camp.kodekorea.kr` 파일 갱신.** 현재 올라간 것은 2026-09-05 11:38
   빌드본이고, 그 뒤 현장 수정(관리자 권한·영상·비전 모델·발표자료 자유화)이
   **반영되지 않았습니다.** 4장 절차대로 교체하세요.
3. **1703 노트북에서 앱이 열리는지 확인.** 안 열리면 그 학생에게는
   여분 노트북을 빌려주는 수밖에 없습니다.
4. **커밋 안 된 변경 정리.** 원본 리포(`C:\Users\user\orca\projects\opencode-remake`)에
   2026-09-05 현장 수정이 커밋되지 않은 상태입니다. 브랜치는
   `handover/20260907` 입니다.
5. **비용 상한 설정.** Google Cloud 예산 알림($150/$300/$500 권장) +
   fal 대시보드 크레딧 확인. `camp-usage.sh` 로 조별·제공자별 집계가 나옵니다
   (`만든 곳별: fal N개 google M개`).
6. **예비 Gemini 열쇠 준비.** 지금 `GEMINI_API_KEY` 하나가 단일 장애점입니다.
   USB 에 예비 키를 담아 두면 현장에서 `~/.config/camp/media-keys.env` 만
   갈아끼울 수 있습니다.

### 캠프 후 반드시

7. **열쇠 전부 폐기.** fal 5개 · Gemini · deepseek. fal 은 대시보드에서
   revoke. 채팅으로 전달된 이력이 있어 특히 중요합니다.
8. **학생 노트북에서 열쇠 회수.**
   맥은 `mac-uninstall.sh`, 윈도우는 `scripts/reset-account.ps1` **(추정: 후자는
   운영자 계정 도구라 학생 노트북에 그대로 쓰기 어려울 수 있습니다. 확인 필요)**
9. **`camp.kodekorea.kr` Caddy 블록 삭제.** 설정 파일에 그렇게 적어 뒀습니다.
10. **LMS 서버의 평문 자격증명 조치** (5장 마지막 항목).

### 알려진 버그 · 검토 대기

- **`camp-usage.sh` 안내문이 오래됐습니다.** "총액은 Google Cloud 예산
  알림으로" 만 적혀 있는데, 이제 **fal 대시보드도 같이 봐야** 전체 지출이
  보입니다.
- **`docs/superpowers/` 설계 문서가 현재 코드와 어긋납니다.**
  특히 "발표자료 7장 골격"과 "macOS 추후" 부분. 참고용으로만 보세요.
- **`README.md` 의 테스트 어서션 개수(bash 211 / PowerShell 154)** 는
  그 시점 숫자입니다. 지금은 파일 수로만 확인했습니다(bash 15 / PS 14, 전부 통과).
- **재시도가 작업을 두 배로 만들 수 있습니다.** 스킬이 exit 5 면 한 번 더
  시도하게 되어 있어, 8분 대기 후 재시도하면 영상 작업 두 개가 돕니다.
  조당 제한으로 상한은 잡혀 있습니다.
- **학생이 노트북을 덮으면** fal 작업은 계속 돌고 요금이 나갑니다. 막을 방법을
  못 찾았습니다.
- **`_보관/04_임시_시험흔적/` 의 `index.html`·`우리팀.md` 는 git 이 추적하던
  파일입니다.** 옮겼기 때문에 `git status` 에 삭제로 보입니다. 되돌리려면
  `git checkout -- index.html 우리팀.md`.

---

## 9. 현장 기록 — 2026-09-05 에 무슨 일이 있었나

후임이 코드의 급한 흔적을 이해하는 데 도움이 될 시간순 기록입니다.

| 때 | 일 |
|---|---|
| 아침 | "애들 다 설치 안 되고 바탕화면에 아무것도 안 생긴다" → 원인은 **스크립트만 고치고 exe 를 안 만든 것**. 5개 exe 재빌드 |
| 오전 | 100Mbps 회선으로 63명이 375MB 를 받으면 죽는다 → **내부망 배포 서버**를 만들어 `192.168.0.5` 로 뿌림 |
| 오전 | 앱이 첫 실행 때 **70MB 를 더 받는다**는 것을 발견(node_modules 61MB + models.json + rg) → **오프라인판** 제작 |
| 점심 | 맥 학생 발생 → 맥용 설치 스크립트 + 공식 파일 453MB 확보 |
| 오후 | "관리자 권한 없어서 문제" → 매니페스트 추가. 그 과정에서 **node MSI 가 유일한 관리자 권한 요구처**임을 확인하고 실패해도 계속하게 바꿈 |
| 오후 | 서브에이전트 5개로 전면 감사 → **영상 100% 실패**(durationSeconds), **무한 루프 2곳**, **동시 생성 덮어쓰기** 발견·수정 |
| 오후 | USB(exFAT) 제작. 원래 Windows 11 설치 미디어였던 것을 포맷 |
| 저녁 | 맥에서 "설정·열쇠가 안 들어감" → **CRLF 사고**. 배포 `.sh` 13개를 LF 로 통일하고 빌드에 검사 추가 |
| 저녁 | Windows 1703 학생 → `ClosePseudoConsole` 확인. 전용 빌드 제작 |
| 저녁 | "도우미가 사진을 못 본다" → **텍스트 전용 모델을 쓰고 있었음**. 도우미만 비전 모델로 |
| 저녁 | Veo 할당량 소진 → **모델별 별도 할당량** 발견, fal 도입, 순서 재조정 |
| 밤 | "애들 웹사이트가 바보같이 나온다" → **템플릿 예시 7장**이 원인. 표지 한 장만 남기고 배경 사진·영상 패턴 제공 |
| 밤 | 이미 설치한 학생에게 수정을 전달할 방법이 없음 → **「도구 고치기」**(20KB) 제작. 도구·프리셋·열쇠를 갈아끼우고 앱을 끄고 새 대화를 열게 함 |

이 기록의 교훈 하나만 남긴다면: **"고쳤다"와 "학생 노트북에 도달했다"는
전혀 다른 일입니다.** 이 프로젝트에서 가장 많이 잃은 시간이 그 간격이었습니다.
`도구 고치기` 경로를 반드시 유지하세요.
