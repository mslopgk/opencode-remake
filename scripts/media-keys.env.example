# 창의디자인캠프 그림·영상 만들기 열쇠.
#
# 이 파일을 dist/secrets/media-keys.env 로 복사하고 값을 채운다.
# 인스톨러가 학생 노트북의 ~/.config/camp/media-keys.env 로 넣어 준다.
# 학생은 이 값을 보지도, 입력하지도 않는다.
#
# 값은 반드시 영문·숫자(ASCII)여야 한다. 한글이나 따옴표를 넣지 않는다.

# 그림 — Cloudflare Workers AI (FLUX.1 schnell)
#   하루 10,000 뉴런 무료. 넘어가도 1,000 뉴런당 $0.011.
#   계정 번호: Cloudflare 대시보드 우측의 Account ID
#   열쇠: My Profile > API Tokens > Create Token
#         권한은 "Workers AI: Read" 만 있으면 된다
CF_ACCOUNT_ID=
CF_API_TOKEN=

# 영상 — fal.ai (MiniMax H3)
#   초당 $0.08. 5초 한 편 $0.40. 팀당 2편 × 15팀 = 약 $12.
#   fal.ai > Dashboard > Keys
FAL_KEY=
